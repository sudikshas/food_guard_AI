# Backend API (FastAPI) — must match the server that connects to RDS (food_recall) and S3 (recallguard-dev-data).
# See ../DATABASE_AND_CONNECTIONS.md for full connection reference.
VITE_API_URL=http://localhost:8000

# Production example:
# VITE_API_URL=http://YOUR-EC2-IP:8000
